{
  "name": "site-pulse",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only proxy between atlas-systems.uk and Cloudflare's GraphQL Analytics API.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "wrangler": "^4.112.0"
  }
}
